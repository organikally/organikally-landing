# SEO / AEO / GEO

Owner: `seo-aeo-expert`. Status: **draft for Phase 1 lock.**
Claims in titles/descriptions/schema must match `COMPLIANCE.md`.

## Strategy

Single high-intent landing page + a small E-E-A-T blog. Win classic SEO (mustard-oil intent)
**and** AEO/GEO — structure answers so generative engines can quote Organikaly on
"cold-pressed vs refined mustard oil", "is mustard oil healthy", "yellow vs black mustard oil".

## Topic / keyword map

**Primary (page):** organic cold-pressed mustard oil; kachi ghani mustard oil; yellow mustard
oil (sarson tel); buy organic mustard oil online India.
**Supporting:** cold-pressed vs refined; MUFA / Omega-3 mustard oil; organic dals / pulses;
organic khand (unrefined jaggery sugar); organic pantry staples India.
**Long-tail / blog:** "cold-pressed vs refined mustard oil"; "how to spot genuinely organic
pantry staples"; "yellow vs black mustard oil"; "mustard oil smoke point / for cooking".

## Heading outline (semantic, one h1)

- `h1`: Organikaly — Cold-Pressed Organic Yellow Mustard Oil (+ brand essence)
- `h2` Trust strip · `h2` The Organikaly story (why authentic) · `h2` Yellow Mustard Oil
  (`h3` benefits, `h3` usage) · `h2` The range (`h3` per category) · `h2` Sourcing & process ·
  `h2` What people say · `h2` Recipes & usage · `h2` Journal · `h2` FAQ · `h2` Buy / enquire

## Metadata

- **Title (~55–60 chars):** "Organikaly — Cold-Pressed Organic Mustard Oil & Pantry"
- **Meta description (~150 chars):** benefit + trust + range, in-voice, no hype.
- **Canonical:** self-referential on `/`; absolute canonicals on blog posts.
- **Open Graph + Twitter:** `og:title/description/image` (logo/lifestyle render, 1200×630),
  `twitter:card=summary_large_image`. OG image generated by the asset pipeline.
- **Lang:** `en-IN`; include Hindi product terms inline (सरसों तेल) for relevance.
- **Alt text:** descriptive, keyword-aware but natural (owned by `copywriter`).

## Structured data (schema.org JSON-LD)

- `Organization` — name, logo, sameAs (socials via MANUAL_STEPS), contactPoint, FSSAI in
  description/identifier where appropriate.
- `Product` — Yellow Mustard Oil: name, image, description, brand, category;
  `offers` only when a real price/URL exists; **nutrition/health properties only as
  `COMPLIANCE.md` allows** (no unverifiable claims in structured data).
- `BreadcrumbList`, `FAQPage` (mirrors the on-page FAQ), `WebSite`.
- Blog posts: `Article`/`BlogPosting` with author, datePublished, about.

## AEO/GEO tactics

- Question-shaped `h2/h3` with concise, self-contained answers directly beneath (extractable).
- Clear entity definition: who Organikaly is, what cold-pressed means here, in plain prose.
- `FAQPage` schema on genuine buyer questions; factual, defensible, citable.
- Fast, crawlable static HTML; no content hidden behind JS-only rendering.

## Technical

- `sitemap.xml` (home + blog) and `robots.txt` (allow, point to sitemap).
- Fast LCP, clean HTML, semantic landmarks → supports SEO score = 100.
- Self-hosted fonts; descriptive link text; no broken links (QA-gated).
